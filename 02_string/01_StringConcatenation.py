# strings

# collection of character  inside single or double quotes

first_name = "chitranjan"

last_name= "Pandit"

#  caocate maens to add multiple string in the single string

full_name = first_name + " " +last_name
print(full_name)

# we can't add a number in the string
# print(first_name + 2) error
print(first_name + str(2)) # no error
print(first_name + "45")

print(first_name *3) # we can multiply but not add
