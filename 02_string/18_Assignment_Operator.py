name = "chittu"
name= name+"it"# name+="it"

# now some people might think how can we change 'name'
# variable if string are immutable?
# remember one thing, in python we canchange value of variable anytime

# but other methos cannot change our string or we cannot change particular character from string(immutability)

print(name)
age=20
age= age+1#age+=1
print(age)